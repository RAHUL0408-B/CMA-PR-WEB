import Database from 'better-sqlite3';
import path from 'path';

const dbPath = 'c:\\Users\\Rahul\\OneDrive\\Documents\\CMA\\CMA\\dev.db';
const db = new Database(dbPath);

try {
    const tableInfo = db.prepare("PRAGMA table_info('User')").all();
    console.log('User table info:', JSON.stringify(tableInfo, null, 2));
} catch (err) {
    console.error('Failed to get table info:', err);
} finally {
    db.close();
}
